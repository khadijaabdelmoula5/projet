{% extends 'base.html.twig' %}

{% block title %}Edit Project{% endblock %}

{% block body %}
<div class="container">

    <h1>Edit Project</h1>
        <div class="card">
            <div class="card-body">
                {{ include('project/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>
<div class="my-3"></div>
        <a href="{{ path('app_project_index', {'id': project.id}) }}" class="btn btn-primary btn-block">back to project list</a>
<br>
        {{ include('project/_delete_form.html.twig') }}
</div>
{% endblock %}
