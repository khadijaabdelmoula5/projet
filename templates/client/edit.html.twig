{% extends 'base.html.twig' %}

{% block title %}Edit Client{% endblock %}

{% block body %}
<div class="container">

    <h1>Edit Client</h1>
        <div class="card">
            <div class="card-body">
                {{ include('client/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>
<div class="my-3"></div>
        <a href="{{ path('app_client_index', {'id': client.id}) }}" class="btn btn-primary btn-block">back to client list</a>
<br>
        {{ include('client/_delete_form.html.twig') }}
</div>
{% endblock %}
