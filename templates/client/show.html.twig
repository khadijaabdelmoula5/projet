{% extends 'base.html.twig' %}

{% block title %}Client{% endblock %}

{% block body %}
    <h1>Client</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ client.id }}</td>
                                <td></td>

            </tr>
            <tr>
                <th>Name</th>
                <td>{{ client.name }}</td>
                                <td></td>

            </tr>
            <tr>
                <th>Address</th>
                <td>{{ client.address }}</td>
                                <td></td>

            </tr>
            <tr>
                <th>Phone_number</th>
                <td>{{ client.phoneNumber }}</td>
                                <td></td>
            </tr>
            <tr>
                <th>Cin</th>
                <td>{{ client.cin }}</td>
                <td></td>
            </tr>
            <tr>
                <th>User</th>
                <td>{{ client.user.login }}  </td>
                <td></td>
            </tr>
             <tr>
                <th><a href="{{ path('app_client_index') }}" class="btn btn-primary btn-block">Back to List</a></th>
                <td> <a href="{{ path('app_client_edit', {'id': client.id}) }}" class="btn btn-success btn-block">Edit</a></td>
                <td>{{ include('client/_delete_form.html.twig') }}</td>
            </tr>
        </tbody>
    </table>

     
{% endblock %}
