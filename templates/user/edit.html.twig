{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
<div class="container">

    <h1>Edit User</h1>
        <div class="card">
            <div class="card-body">
                {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}
            </div>
        </div>

        <a href="{{ path('app_user_index', {'id': user.id}) }}" class="btn btn-danger">back to list</a>

        {{ include('user/_delete_form.html.twig') }}
</div>
{% endblock %}
