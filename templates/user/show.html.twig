{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
                                <td></td>

            </tr>
            <tr>
                <th>Login</th>
                <td>{{ user.login }}</td>
                                <td></td>

            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
                <td></td>

            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
                 <td></td>
            </tr>
             <tr>
                <th><a href="{{ path('app_user_index') }}" class="btn btn-primary btn-block">Back to List</a></th>
                <td> <a href="{{ path('app_user_edit', {'id': user.id}) }}" class="btn btn-success btn-block">Edit</a></td>
                <td>{{ include('user/_delete_form.html.twig') }}</td>
            </tr>
        </tbody>
    </table>
    
{% endblock %}
